name: vk_engine_churn_repro
description: "MRE harness for flutter/flutter#191168: Impeller Vulkan vkDestroyFramebuffer crash under background FlutterEngine churn. Zero third-party dependencies: plain Android JobScheduler plus the Flutter embedding API."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
